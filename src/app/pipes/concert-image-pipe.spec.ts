import { ConcertImagePipe } from './concert-image-pipe';

describe('ConcertImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ConcertImagePipe();
    expect(pipe).toBeTruthy();
  });
});
